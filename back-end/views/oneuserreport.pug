html
  head
    title= title
  body
    h1= message
    br
    form(action=url, method='POST')
      p
      |
      input(type='hidden' name='token' value=token)
      |
      input(type='submit' name='menu' value='menu') 

    
    ul 
        form(action=`/admin/handleReport/${rep.id}`, method='POST')
            p
            strong= `${reporter.firstName} ${reporter.lastName} `
            body= `reports `
            strong=`${accused.firstName} ${accused.lastName}` 
            br
            body= "Description: " + rep.description    
            br
            input(type='submit' name='dismiss' value='dismiss')    
            input(type='submit' name='ban' value='ban')
            input(type='hidden' name='token' value=token)